# Definition of Done

每个 Codex 任务只有同时满足以下条件才允许标记完成：

1. 需求和验收标准逐条映射到代码、测试与证据；没有“以后补”的隐藏占位符。
2. 未修改 `frappe`、`erpnext`、`hrms` 核心源码；标准对象扩展遵循 Custom Field/fixtures → `extend_doctype_class` → `doc_events` → 领域服务的优先级。
3. 所有写操作完成权限、数据范围、不相容职责、输入校验、幂等、事务、审计和异常处理。
4. 核心台账只能追加和冲销，不允许更新历史金额；不得直接写 `GL Entry`、`Stock Ledger Entry`、`Bin` 等标准台账表。
5. 单元测试、集成测试、权限测试通过；涉及工作流/API/台账的任务还需端到端、并发或冲销测试。
6. `ruff`、`pyright`、前端 lint、依赖漏洞扫描和秘密扫描通过。
7. DocType JSON、fixtures、迁移 patch、文档、报表和权限同步提交；多语言文本使用翻译机制。
8. 数据迁移和回滚路径明确；破坏性变更有 ADR，且升级测试覆盖当前锁定的 Frappe 版本。
9. 日志不包含密码、令牌、完整身份证号、银行账号、患者敏感信息或未经脱敏的AI输入。
10. PR 描述包含：任务ID、设计依据、变更范围、测试证据、迁移影响、回滚方法和截图/接口样例。
