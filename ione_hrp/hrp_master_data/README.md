# HRP Master Data

**领域组：** Core

**中文名称：** 主数据管理

主数据申请、审核、发布、映射、版本、质量与数据责任体系。

COD-020 已实现受控主数据域和结构化变更申请。当前支持 `Department`、
`Cost Center`、`Item`、`Supplier`、`Warehouse` 五类 ERPNext 标准主数据；
字段白名单由源码策略注册表管理。申请必须绑定已发布组织版本中的有效组织单元，
采用草稿、提交、复核状态机和申请人/审核人职责分离。审批只确认提案，不直接修改
ERPNext 标准记录。
