# COD-016 建立安全扫描和SBOM

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/single_app_architecture.md`
- `architecture/software_supply_chain.md`
- `architecture/adr/ADR-0011-governed-build-time-sbom-and-security-gates.md`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`
- 锁定 ERPNext `372dff2ffa232f54595f48c2639d828c4a64ddde`
- 锁定 Frappe HR `2d9e4a0bc7a8d18c42c25cc7ff95eb52b460c6b1`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 严格策略、工具、门禁、例外、SBOM 和扫描摘要模型 | 单元测试 |
| 服务 | 策略加载、脱敏状态和 `HRP Foundation` facade | 锁定 Bench 测试 |
| 权限 | 系统管理角色且在策略读取前校验 | 权限先行测试 |
| API | `PLT-019` GET-only 治理契约 | 三份目录与 HTTP 测试 |
| SBOM | CycloneDX 1.7，含应用、三项上游、Python 和 Node 组件 | CLI 验证与 SHA-256 |
| 扫描 | Bandit、Gitleaks、pip-audit、npm audit、Grype | `Security` CI 作业 |
| 隔离 | 无站点执行、无生产执行、子进程环境白名单 | 门禁和负路径测试 |
| 制品 | 只上传 SBOM、脱敏摘要和校验清单 | CI Artifact |
| 文档 | ADR、架构、任务、变更、运行和回滚说明 | Git 治理门禁 |

## 实现边界

不创建 DocType、Fixture、Patch、Workspace 或第二个自研 App。Frappe Site 只能
读取治理契约，不能启动扫描、生成 SBOM、上传原始报告或写入扫描结果。

扫描不连接 Press、生产数据库或外部业务系统。原始发现只存在于临时 CI 工作区。

## 验证

```bash
python -m unittest tests.test_software_supply_chain -v
python scripts/security_supply_chain.py plan
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-016 \
  --correlation-id COD-016-local
python scripts/quality.py
```

真实安全流水线还必须使用固定工具运行 `security_supply_chain.py run`，通过
CycloneDX 1.7 校验和全部零容忍门禁。锁定 Bench 集成测试必须覆盖角色、异常、
无写入、脱敏审计和 `PLT-019` HTTP 成功/访客拒绝。

## 迁移与回滚

无数据库迁移。回滚应用提交和 CI 配置即可。`.artifacts/security` 与 CI 制品不进入
Site 数据库，删除制品不影响业务数据。

## 实施证据

- Pull Request：[#21](https://github.com/jerry317395616/ione_hrp/pull/21)
- 实现提交：`45d68607dba71d234899d70f223fd7d8e9c22e2e`
- 集成修正提交：`395692c023d8d4f17e537342f08eb79886ee4974`
- GitHub Actions：
  [30511020508](https://github.com/jerry317395616/ione_hrp/actions/runs/30511020508)
- Quality、Security、Integration 和 Required 四个作业全部通过。
- Security 作业生成并验证 CycloneDX 1.7 SBOM，包含 105 个组件和 105 条依赖，
  Bandit、Gitleaks、pip-audit、npm audit、Grype 与许可证门禁均为零阻断发现。
- Integration 作业在三个锁定上游提交上通过 100 个 Frappe 集成测试、重复迁移和
  三个隔离站点校验，三个站点的 `Error Log` 记录均为 0。
- 本地 `scripts/quality.py` 通过 186 个仓库契约与单元测试；真实本地安全流水线
  也通过全部门禁。
