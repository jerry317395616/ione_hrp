# COD-012 建立不可变台账基类

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/single_app_architecture.md`
- `architecture/domain_services.md`
- `architecture/immutable_ledgers.md`
- `architecture/adr/ADR-0007-immutable-ledger-base.md`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 不可变 Ledger 定义、17 个公共字段和确定性维度快照 | 纯单元测试与蓝图门禁 |
| 服务 | 追加、行锁冲销和一次冲销模板 | MariaDB 双连接集成测试 |
| 数据 | 不创建通用生产 Ledger DocType | 测试临时 DocType 自动清理 |
| 权限 | 具体 DocType 只读，写入仅领域服务上下文 | CRUD 和权限先行测试 |
| API | `PLT-015` 只读公共契约 | CSV、YAML、OpenAPI 与 HTTP 测试 |
| 审计 | 追加、冲销和领域服务生命周期事件 | mock 日志脱敏断言 |
| 幂等 | 追加和冲销复用 COD-011 加密持久化幂等 | 成功、重放、冲突和回滚测试 |
| 异常 | 不存在、非法状态、锁竞争和非法载荷使用统一错误 | 集成测试 |
| 文档 | 架构说明、ADR、任务和变更记录 | Git 治理门禁 |

## 实现边界

本任务只交付公共基类，不实现 COD-033 预算台账或后续科室库存业务。具体领域必须保留
自己的结构化 DocType，并通过公开 service facade 调用基类。不得创建万能 JSON 台账，
不得直接写 ERPNext `GL Entry`、`Stock Ledger Entry` 或 `Bin`。

## 验证

```bash
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-012 \
  --correlation-id COD-012-local
python scripts/quality.py
bash -n scripts/ci_integration.sh
```

锁定 Bench 集成测试必须覆盖真实 MariaDB 行锁、冲销、重复冲销、直接 CRUD、权限、
持久化幂等、异常回滚和 HTTP 端到端权限。

## 迁移与回滚

本任务无生产 DocType 或业务数据迁移。常规 migrate 用于同步 Python/API 元数据。回滚
应用提交即可，不能删除后续具体业务台账；具体台账的数据迁移和回滚由对应 COD 负责。

## 实施证据

实现提交、GitHub Actions、测试数量和合并证据在 CI 完成后补充。
