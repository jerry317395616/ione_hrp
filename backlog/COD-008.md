# COD-008 建立 ADR 和变更治理

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/repository_governance.md`
- `architecture/single_app_architecture.md`
- `architecture/adr/ADR-0002-source-controlled-change-governance.md`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| ADR 模型 | 严格 front matter、固定章节和状态机 | 非法字段、章节和转换测试 |
| 变更模型 | 每个已完成 COD 的结构化记录 | backlog、任务文档、模块和 ADR 交叉校验 |
| 风险治理 | 关键路径最低风险与 Accepted ADR | 低报风险、破坏性和未覆盖路径拒绝 |
| 单任务 PR | 真实 Git name-status 差异 | 恰好一个变更记录、任务文档和 backlog |
| 权限/API | 管理员只读脱敏状态 | HTTP 成功、Guest/普通用户拒绝 |
| 审计 | Git/PR/CI + 本地脱敏 JSONL | 关联 ID、SHA、权限与敏感标记测试 |
| 幂等 | 只读解析与确定性 SHA-256 | 重复计划摘要一致 |
| CI | PR base 与 main before commit 检查 | Quality 作业真实差异门禁 |

## 模型、服务、权限和迁移

本任务不新增业务 DocType、Fixture 或数据库 Patch。ADR 和工程变更必须在应用
安装与迁移之前完成评审，因此机器模型位于版本控制中的策略、Markdown ADR 和
JSON 变更记录，不能建立一个可在生产 Site 中绕过 PR 修改的第二权威。

- 核心：`ione_hrp.common.change_governance` 严格解析、交叉校验和生成摘要。
- 服务：`ione_hrp.services.change_governance` 只读返回脱敏状态并记录请求事件。
- API：GET，仅 `System Manager` 或 `HRP System Manager`，无 HTTP 写接口。
- CLI：`scripts/change_manager.py` 校验完整仓库和真实 Git 差异。
- 数据：无表结构和业务数据变更，不触发会计、库存或 HR 过账。
- 生产：不连接 Press、不修改 `manager.myyr.top`，无生产迁移。

## 安全、审计与幂等

- API 不返回绝对路径、ADR 正文、决策人、命令输出或环境变量。
- CLI 审计不保存凭据、业务数据或文件内容，POSIX 权限固定为 `0600`。
- Accepted ADR 内容不可改写；删除、改名和非法状态跳转失败关闭。
- 破坏性、单应用边界、权限、上游基线和治理门禁变更强制 Accepted ADR。
- 相同仓库和相同差异重复评估产生相同治理 SHA 与变更 SHA。

## 验证

```bash
python scripts/change_manager.py validate
python scripts/change_manager.py plan --base-ref main --task COD-008
python scripts/change_manager.py check \
  --base-ref main \
  --task COD-008 \
  --correlation-id COD-008-local
python scripts/quality.py
bash -n scripts/ci_integration.sh
```

锁定 Bench 集成测试必须覆盖 Frappe API 的管理员 HTTP 成功路径、Guest 和普通
用户拒绝、非法关联 ID、脱敏响应、确定性摘要和只读行为。

## 回滚

回滚本任务代码只移除治理门禁和只读状态 API，不删除 Git、PR、CI 审计，不
修改 Site 数据。已经 Accepted 的 ADR 不通过历史改写回滚；后续决策使用新的
ADR 取代。已实施代码需要回滚时创建新的 COD，并把原变更记录保留为
`Rolled Back`。
