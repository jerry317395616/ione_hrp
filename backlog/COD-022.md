# COD-022 实现主数据质量规则

## 设计依据

- `AGENTS.md`
- `architecture/single_app_architecture.md`
- `architecture/data_quality_rules.md`
- `architecture/adr/ADR-0016-declarative-master-data-quality-rules.md`
- `design/doctype_catalog.csv`
- `design/field_catalog.csv`
- `doctype_blueprints/hrp_master_data/hrp_data_quality_rule.json`
- `doctype_blueprints/hrp_master_data/hrp_data_quality_issue.json`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`
- 锁定 ERPNext `372dff2ffa232f54595f48c2639d828c4a64ddde`
- 锁定 Frappe HR `2d9e4a0bc7a8d18c42c25cc7ff95eb52b460c6b1`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 声明式质量规则及自动问题生命周期 | 运行时元数据、蓝图、设计目录 |
| 规则 | 五类白名单操作符和固定命名格式 | 纯契约和集成测试 |
| 服务 | 规则修订、单条评估、解决/重开、批处理 | Frappe 集成测试 |
| 权限 | 治理角色使用、Desk 只读、Guest 拒绝 | 权限钩子和越权测试 |
| API | `CORE-023`、`CORE-024`、`CORE-025` | 三份 API 目录和真实 HTTP 测试 |
| 并发 | 行锁、乐观修订、问题唯一约束 | 冲突和重复迁移测试 |
| 幂等 | 写命令及每日目标评估使用稳定请求键 | 重放和缺失请求头测试 |
| 审计 | 只记录摘要、状态、失败代码和修订 | 敏感原文排除测试 |
| 调度 | 每日规则扫描、每批最多 200 条 | 分页续排测试 |
| 工作区 | 中文“数据质量规则/问题”入口 | Workspace 和 HRP 侧栏测试 |

## 实现边界

只修改 `ione_hrp` 的 `HRP Master Data` 模块和公共契约。标准主数据仅做带绑定参数的读取和锁定，不创建、
修改或删除 ERPNext 记录。不接受用户 SQL、Python、任意正则或网络调用。不创建第二个 App，不修改三个
锁定上游，不连接生产 Press。

## 验证

```bash
python -m unittest tests.test_data_quality -v
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-022 \
  --correlation-id COD-022-local
python scripts/quality.py
```

锁定 Bench 必须运行 `ione_hrp.hrp_master_data.tests.test_data_quality` 和全量应用测试，并验证
development、test、demo 三个隔离站点重复迁移无漂移且 `Error Log` 为零。

## 迁移与回滚

迁移新增两个 DocType 表、一个规则调度索引、一个问题唯一约束和一个问题状态索引，不修改 ERPNext 标准
记录。升级前备份站点数据库；回滚应用提交后迁移，保留表、规则、问题和摘要，不执行破坏性删除。

## 实施证据

- 分支：`cod-022-master-data-quality-rules`
- 功能提交：`713cbaa8650357eb91db8851e4aa3f6229194967`
- 纯契约测试：8 项通过；仓库测试共 240 项通过
- Frappe 集成测试：161 项通过，其中 COD-022 新增 9 项
- 隔离环境：development、test、demo 配置无漂移；test 重复配置幂等；三站点 `Error Log` 均为 0
- GitHub PR：[#28](https://github.com/jerry317395616/ione_hrp/pull/28)
- CI：[运行 31236760832](https://github.com/jerry317395616/ione_hrp/actions/runs/31236760832)，Quality、Security、Integration、Required 全部通过
- 生产边界：未连接或修改生产 Press、Bench、Site 和数据库
