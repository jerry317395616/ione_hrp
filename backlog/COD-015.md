# COD-015 建立性能基线与压测脚本

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/single_app_architecture.md`
- `architecture/environments.md`
- `architecture/test_data_factory.md`
- `architecture/performance_baselines.md`
- `architecture/adr/ADR-0010-external-governed-performance-baseline.md`
- `design/tests.csv` TST-0133 至 TST-0147
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`
- 锁定 ERPNext `372dff2ffa232f54595f48c2639d828c4a64ddde`
- 锁定 Frappe HR `2d9e4a0bc7a8d18c42c25cc7ff95eb52b460c6b1`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 场景、执行档、资源上限、阈值、结果模型和独立评估器 | 纯单元测试 |
| 服务 | 注册表加载、环境判定和 `HRP Foundation` 公共 facade | 锁定 Bench 测试 |
| 权限 | 两类系统管理员；角色在注册表和环境读取前校验 | 权限先行测试 |
| API | `PLT-018` 只读性能和环境许可契约 | CSV、YAML、OpenAPI、HTTP 测试 |
| 幂等 | GET 重复读取结果一致且不产生版本、评论、错误或幂等记录 | 无写入集成测试 |
| 审计 | 只记录注册表 SHA、场景数和许可布尔值 | 站点、用户、路径、目标脱敏断言 |
| 压测 | k6 固定迭代脚本和 Python 安全编排/结果复核 | 源码门禁、dry-run 与结果测试 |
| 文档 | ADR、架构、任务、变更和运行/回滚手册 | Git 治理门禁 |

## 实现边界

本任务不创建 DocType、Fixture、Patch 或 Workspace。站点内不启动压测，也不接受结果
写入；场景只允许 `ione_hrp` 的 GET 路径。初始基线不实现尚未交付的预算、库存、三单
匹配、故障注入和安全场景，只建立它们后续必须复用的执行契约。

## 验证

```bash
python -m unittest tests.test_performance_baseline -v
python scripts/performance_baseline.py \
  --base-url http://127.0.0.1:8200 \
  --profile smoke \
  --dry-run
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-015 \
  --correlation-id COD-015-local
python scripts/quality.py
```

锁定 Bench 集成测试必须覆盖系统角色、危险环境、配置异常、重复读取无写入、脱敏审计以及
`PLT-018` 的真实 HTTP 成功和访客拒绝。实际容量测试只在隔离的生产等价 test 环境运行，
不得连接 Press 生产 Site。

## 迁移与回滚

常规 migrate 不新增表或生产数据。回滚应用提交即可；`.artifacts/` 报告不进入 Git 或
Site 数据库，按制品保留策略删除。执行后吊销专用测试 API Token。

## 实施证据

- Pull Request：[PR #20](https://github.com/jerry317395616/ione_hrp/pull/20)
- 实现提交：`3655e532cefdf03a22c9997be372fe755d566233`
- GitHub Actions：[run 30498686457](https://github.com/jerry317395616/ione_hrp/actions/runs/30498686457)
- Quality：171 项仓库测试、Pyright/ESLint/Prettier、官方 k6 `2.1.0` SHA 校验与
  `k6 inspect`、Gitleaks 和依赖审计全部通过。
- Integration：锁定三项上游提交、连续迁移、93 项 Frappe 集成测试和三个隔离 Site
  Error Log 为 0；其中 COD-015 新增 8 项权限、环境、幂等、审计和 HTTP 测试。
- 本地真实 smoke：k6 `2.1.0` 固定 5 请求全部通过，错误率 0、检查率 100%，Python
  独立评估无违规；危险 `K6_*` 父环境变量被白名单隔离。
