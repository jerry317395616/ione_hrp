# COD-025 实现审批矩阵解析器

## 任务信息

- 阶段：1-核心闭环
- 模块：HRP核心平台 / HRP Workflow Authorization
- 优先级：P0
- 前置任务：COD-024
- 风险：审批越权、规则歧义、金额与组织上下文篡改、生产迁移

## 验收映射

| 验收项 | 实现 | 自动化证据 |
| --- | --- | --- |
| 审批策略模型 | `HRP Approval Matrix` 与步骤子表 | 元数据、蓝图和字段目录契约 |
| 确定性解析 | 连续阈值、并行会签、唯一最高优先级 | 纯解析与真实单据集成测试 |
| 最小权限 | DocPerm 与组织读写范围同时成立 | 用户、角色、范围和 Guest 测试 |
| 受控 API | `CORE-002`、`CORE-028`、`CORE-029` | HTTP、幂等、乐观锁和只读测试 |
| 审计 | 策略与决策摘要，不记录敏感原值 | 服务审计测试与仓库契约 |
| 迁移与入口 | 三类索引和中文权限工作区 | 重复迁移与 Workspace 映射测试 |

## 实现范围

- 用正式运行模型替换通用设计占位；
- 支持组织、日期、金额、优先级和声明式额外维度；
- 支持角色/用户审批人、并行审批与 All/Any 模式；
- 评估真实目标单据并验证调用方和审批人的数据范围；
- 提供幂等维护接口、只读评估/查询接口和摘要审计。

## 非目标

- 不直接提交、批准或修改业务单据；
- 不替代 Frappe Workflow；
- 不允许 Python、SQL、Server Script 或任意表达式策略；
- 不修改 Frappe、ERPNext 或 Frappe HR 核心源码。

## 验证命令

```bash
python scripts/quality.py
bench --site <test-site> migrate
bench --site <test-site> run-tests --app ione_hrp --module ione_hrp.hrp_workflow_authorization.tests.test_approval_matrix
```

## 状态

Done

## 实现证据

- 功能分支：`cod-025-approval-matrix`
- 候选提交：`a613c8c4cb8a15cbe4319c81042ddb4bc91cfc0c`
- Pull Request：[#32](https://github.com/jerry317395616/ione_hrp/pull/32)
- GitHub Actions：[CI 31663536932](https://github.com/jerry317395616/ione_hrp/actions/runs/31663536932)
- 主干合并提交：`ff35587e8323873519a630810ef3d54ded9c75ea`
- 质量门禁：274 项仓库测试、Ruff、Bandit、Pyright、ESLint、Prettier、SBOM、依赖和密钥扫描全部通过
- Frappe 集成：锁定 MariaDB Bench 全量测试通过；审批矩阵 9 项集成测试及重复迁移通过
- 数据保护：部署前完整备份位于 `/home/zyd/frappe-migration-backups/pre-cod025-20260813-120124`，数据库、公共文件、私有文件和站点配置均通过格式与 SHA-256 校验
- 正式发布：直接 Docker Compose Bench 的 6 个应用服务运行合并提交 `ff35587`；`manager.myyr.top` 连续两次迁移成功，调度器启用且维护模式关闭
- 上线核验：17 类核心业务数据计数与升级前一致；2 个 DocType、3 个复合索引、工作区入口与角色权限通过；Guest API 返回 403；迁移后 Error Log 为 0
- 回滚演练：首次就绪探针误判时自动恢复旧镜像和站点服务，未执行迁移；修正探针后重新完整备份并成功发布
