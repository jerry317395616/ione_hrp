# COD-002 锁定 Frappe、ERPNext、Frappe HR 版本与 SHA

## 设计依据

- `AGENTS.md`
- `resolved_versions.lock.json`
- `architecture/version_baseline.json`
- `architecture/deployment.md`

## 适用范围

本任务是工程基线任务，不新增业务 DocType、写服务、Workspace、Fixture 或
数据库迁移。新增一个认证后可读的诊断 API；权限异常路径由自动化测试覆盖。
版本锁不会写业务数据，也不会直接修改 Frappe、ERPNext 或 Frappe HR 源码。

## 锁定基线

| App | 官方分支 | 锁定 SHA | 版本 |
| --- | --- | --- | --- |
| Frappe | `frappe/frappe:develop` | `883224ff626c4635a58c47133f1ba6101cdbd938` | `17.0.0-dev` |
| ERPNext | `frappe/erpnext:develop` | `372dff2ffa232f54595f48c2639d828c4a64ddde` | `17.0.0-dev` |
| Frappe HR | `frappe/hrms:develop` | `2d9e4a0bc7a8d18c42c25cc7ff95eb52b460c6b1` | `17.0.0-dev` |

三个 SHA 于 `2026-07-29T02:10:04Z` 同批解析。`develop` 后续推进不自动改变
本项目基线。

## 交付

- `resolved_versions.lock.json`：唯一机器权威。
- `scripts/version_lock.py`：校验锁结构、官方远程、提交、版本和工作树。
- 初始化与已有 Bench 安装脚本在安装 `ione_hrp` 前执行严格验证。
- `ione_hrp.api.v1.health.get_upstream_version_status`：认证后的只读诊断 API。
- `ione_hrp_enforce_upstream_lock=1`：候选 Bench 对齐后才启用的迁移闸门。
- 仓库契约强制架构基线与锁文件一致，拒绝占位 SHA 和非官方来源。

## 验证

```bash
python scripts/version_lock.py
python scripts/version_lock.py --verify-remote-heads
python scripts/repository_contract.py
python scripts/validate_package.py
python -m unittest discover -s tests -p "test_*.py"
python -m compileall -q ione_hrp scripts tests
python -m ruff check ione_hrp scripts tests
```

隔离 Bench 还必须完成：

```bash
python scripts/version_lock.py --bench /path/to/bench
bench new-site <test-site>
bench --site <test-site> install-app erpnext
bench --site <test-site> install-app hrms
bench --site <test-site> install-app ione_hrp
bench --site <test-site> migrate
bench --site <test-site> run-tests --app ione_hrp
```

## 生产差异与推广

推广前，`manager.myyr.top` 的 Press App Source 使用
`jerry317395616/{frappe,erpnext,hrms}:manager-develop`，其中 Frappe 和 HRMS
包含本地快照提交。审计确认 Frappe 仅有设置向导中国默认值补丁、HRMS 仅有
安装导入兜底补丁，ERPNext 无自定义提交；新候选不携带这些核心源码修改。

为生产创建并固定以下 fork 分支，旧分支保留用于回滚：

- `jerry317395616/frappe:ione-v17-lock-20260729`
- `jerry317395616/erpnext:ione-v17-lock-20260729`
- `jerry317395616/hrms:ione-v17-lock-20260729`

## 隔离验证结果

- 时间：`2026-07-29T02:35:10Z`
- Bench：`/home/zyd/validation/cod-002-bench`
- Site：`cod002.localhost`
- Python：`3.14.6`
- 三个上游 App 均为官方远程、分离 HEAD、锁定 SHA、干净工作树。
- Frappe 新站点、ERPNext、Frappe HR、`ione_hrp` 安装成功。
- 启用 `ione_hrp_enforce_upstream_lock=1` 后再次迁移成功。
- 诊断 API 返回 `status=match` 且无问题。
- Frappe 集成测试 `2/2` 通过，包含 Guest 拒绝路径。
- 临时数据库管理员已撤销，临时密码文件已删除，专用 Redis 已停止。

## 生产推广结果

- 时间：`2026-07-29`
- Press Release Group：`bench-0002`
- Deploy Candidate：`deploy-0002-000027`
- Build：`fp4bt4i1r2`
- Docker 镜像：
  `registry.fc.myyr.top:15500/i-one/myyr.top/bench-0002:fp4bt4i1r2`
- 新 Bench：`bench-0002-000027-f1`
- Site：`manager.myyr.top`
- App：19 个，包含 `ione_hrp` 提交
  `757b3a7974443b68684915f4c382494f9e878ee8`
- 新 Bench 创建任务 `248` 的四个阶段全部成功。
- Site Update `hfe860f3or` 使用 `Migrate` 和 Press 逻辑备份，最终状态
  `Success`；站点已切换到新 Bench，旧 Bench `bench-0002-000026-f1`
  已归档。

生产升级前保留了数据库、公开文件、私有文件和站点配置完整备份：

| 文件 | SHA-256 |
| --- | --- |
| `20260729_124229-manager_myyr_top-database.sql.gz` | `32a68c5e7ab6db19b1580db2ada65ec50036012e1236998cc8b42dd9a9db7333` |
| `20260729_124229-manager_myyr_top-files.tgz` | `38780022168434368d1c79064415221fd2fbcc96bbf24de8988698f15cab24d9` |
| `20260729_124229-manager_myyr_top-private-files.tgz` | `76fb5fa9b19a16868c54f06f5162abbf9a83d1c4032b392cc8f8579f8aff7dc4` |
| `20260729_124229-manager_myyr_top-site_config_backup.json` | `0ed51c884a879fad0e5d4e5892d056a2b6cbe137aded50567937a21791050267` |

## 生产验收

- 候选镜像先导入独立 QA 数据库，连续两次执行
  `migrate --skip-search-index` 均成功。
- QA 迁移只新增 `tabPrint Format Snippet`、`tabProforma Invoice` 和
  `tabProforma Invoice Item`，未删除表。
- 生产与 QA 均为 1364 张表，排序后表名 SHA-256 均为
  `e271719b6ab4f8b88c642e7298423155569e1ba770b02abc451b380b0d448c79`。
- 16 个核心业务 DocType 以及全部 I-ONE/HRP DocType 的记录数逐项一致。
- 生产启用 `ione_hrp_enforce_upstream_lock=1` 后再次迁移成功。
- 运行时诊断返回 `status=match`、`issues=[]`，三个上游版本均为
  `17.0.0-dev` 且提交与锁文件完全一致。
- `https://manager.myyr.top/api/method/ping` 返回 HTTP 200；诊断 API
  对 Guest 返回 HTTP 403。
- 维护模式关闭，Scheduler 启用，2 个 Worker 在线。
- QA 容器、独立数据库和数据库用户在验收后已清理。
- 验收后发现早先并发构建的未锁定候选 `deploy-0002-000028` 创建了空
  Bench，但没有 `Bench Update`、`Site Update` 或站点。为避免自动更新器
  后续选择该候选，已通过 Press 正式归档任务 `255` 将
  `bench-0002-000028-f1` 归档；生产 Bench 未改变，Agent 队列最终为空。

迁移期间 Agent Job 被重复投递：首次任务 `250` 已完成备份、移动、迁移、
关闭维护模式和搜索索引构建，全部命令返回 0；重复请求仍指向已移走站点的
旧 Bench，返回 404 并使 Press 短暂误标失败。恢复任务未执行回滚。随后以
Agent 任务 `250` 的真实成功终态调用 Press 自带轮询处理器完成状态协调，
清除误设的 Fatal 标记并重新验收。最终 Agent Job、Site Update、Site 和
目标 Bench 状态分别为 `Success`、`Success`、`Active`、`Active`。

COD-002 状态：`Done`。

## 回滚

- 本任务源码回滚不会变更业务数据。
- 生产回滚使用上述完整备份和上一 Deploy Candidate
  `deploy-0002-000026`。
- 禁止直接修改 `tabInstalled Applications`、数据库表或 Git 元数据伪造匹配。
