# COD-023 实现统一编号服务

## 任务信息

- 阶段：1-核心闭环
- 模块：HRP核心平台 / HRP Foundation
- 优先级：P0
- 前置任务：COD-022
- 风险：关键业务基础设施、并发、幂等、数据不可变和权限

## 验收映射

| 验收项 | 实现 | 自动化证据 |
| --- | --- | --- |
| 统一编号方案 | `HRP Numbering Scheme`、受控模板契约、乐观修订服务 | 纯契约和 Frappe 元数据/修订测试 |
| 原子发号 | `tabSeries` 原子递增、事务行锁、全局编号唯一约束 | 连续分配、维度/月份隔离和碰撞回滚测试 |
| 不可变预留 | `HRP Number Reservation` 服务专属创建和修改/删除守卫 | 直接写入、保存和删除拒绝测试 |
| 幂等 | `DomainService` 加密响应快照 | 同键重放不新增预留、异载荷冲突测试 |
| 权限与范围 | 管理、分配、审计角色及预留所有者过滤 | Guest、普通用户、HRP用户和审计员测试 |
| API | CORE-006、CORE-026、CORE-027 | 真实 HTTP 创建、发号、重放、查询和缺失请求头测试 |
| 审计 | 只记录摘要、修订、分桶、序号 | 日志不含完整编号和维度原文测试 |
| 迁移与工作区 | 幂等索引迁移、HRP 首页中文入口 | 重复迁移、索引和默认工作区映射测试 |

## 实现范围

- 新增统一编号纯契约、方案与预留 DocType、领域服务、权限、API 和迁移；
- 替换设计目录中的通用编号方案占位模型；
- 新增不可变编号预留模型；
- 更正 CORE-006 的历史 `ione_hrp_core` 路径并补充方案维护和预留查询 API；
- HRP 首页新增“编号方案”和“编号预留”，保留组织、主数据和系统配置入口；
- 不修改 Frappe、ERPNext、Frappe HR，不连接或变更 Press 生产站点。

## 非目标

- 不为各业务 DocType 自动接线；后续业务任务按需调用 CORE-006；
- 不实现 COD-024 的统一数据权限解析器；当前采用角色和预留所有者边界；
- 不回收已发编号，不保证无空洞连续；
- 不允许自定义代码、SQL、正则或表达式模板。

## 验证命令

```bash
python scripts/quality.py
bench --site <test-site> migrate
bench --site <test-site> run-tests --app ione_hrp
```

## 状态

Done

## 实现证据

- 功能分支：`cod-023-unified-numbering-service`
- 功能基线提交：`1dd548d4489655c13148ea1dac80e8b39cc3c389`
- Pull Request：[#29](https://github.com/jerry317395616/ione_hrp/pull/29)
- GitHub Actions：[CI 31583553929](https://github.com/jerry317395616/ione_hrp/actions/runs/31583553929)
- 质量门禁：258 项仓库契约测试通过，Ruff、Bandit、Pyright、ESLint、Prettier 和供应链安全检查通过
- Frappe 集成：174 项测试通过，其中统一编号 13 项；开发、测试和演示站点重复迁移通过，错误日志均为 0
- 发布边界：未连接或修改生产 Press、`manager.myyr.top` 及其业务数据；合并后沿用现有 Press 发布流程
