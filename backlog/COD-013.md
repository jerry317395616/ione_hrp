# COD-013 建立 Outbox / Inbox 基类

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/single_app_architecture.md`
- `architecture/domain_services.md`
- `architecture/transactional_messages.md`
- `architecture/adr/ADR-0008-transactional-outbox-inbox.md`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 23 个公共字段、双状态机、规范 JSON、哈希与确定性名称 | 纯单元测试 |
| 服务 | Outbox 发布/领取/成功/失败与 Inbox 开始/完成/失败基类 | 锁定 Bench 测试 |
| 数据 | 不创建通用生产 Outbox / Inbox | 临时 DocType 自动清理 |
| 权限 | 具体 DocType 只读，写入仅服务上下文 | CRUD 与权限先行测试 |
| API | `PLT-016` 只读公共契约 | CSV、YAML、OpenAPI 与 HTTP 测试 |
| 审计 | 生命周期、状态、尝试次数和受控错误码 | 脱敏日志断言 |
| 幂等 | 所有命令复用 COD-011 加密持久化幂等 | 重放、冲突和回滚测试 |
| 并发 | MariaDB `FOR UPDATE NOWAIT`、租约与令牌哈希 | 双连接集成测试 |
| 文档 | 架构说明、ADR、任务和变更记录 | Git 治理门禁 |

## 实现边界

本任务只交付平台基类，不实现外部系统配置、网络客户端、通用接收接口或调度器。具体
`HRP Integration Inbox` 和 `HRP Integration Outbox` 由 COD-102、COD-103 在
`HRP Integration` 模块落地。业务模块只能通过公开服务 facade 发布和消费，不得直接
CRUD 消息表。

## 验证

```bash
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-013 \
  --correlation-id COD-013-local
python scripts/quality.py
bash -n scripts/ci_integration.sh
```

锁定 Bench 集成测试必须覆盖真实 MariaDB 行锁、租约令牌、持久化幂等、权限先行、异常
回滚、Outbox 重试/死信、Inbox 去重/冲突和真实 HTTP 端到端权限。

## 迁移与回滚

本任务无生产 DocType、Workspace、Fixture、表或数据 Patch。常规 migrate 仅同步
Python/API 元数据。回滚应用提交即可；后续具体消息表上线后的数据迁移、保留与回滚由
对应 COD 负责。

## 实施证据

- 实现提交：待提交
- Pull Request：待创建
- GitHub Actions：待运行
- 本地仓库测试：待运行
- 锁定 Bench 集成测试：待运行
- 生产影响：不连接 Press，不迁移生产 Site，不新增生产 DocType 或业务表
