# COD-019 实现组织与标准 Department/Cost Center 映射

## 设计依据

- `AGENTS.md`
- `architecture/single_app_architecture.md`
- `architecture/organization_hierarchy.md`
- `architecture/organization_mapping.md`
- `architecture/adr/ADR-0013-version-scoped-standard-organization-mapping.md`
- `design/doctype_catalog.csv`
- `design/field_catalog.csv`
- `doctype_blueprints/hrp_organization/hrp_organization_mapping.json`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`
- 锁定 ERPNext `372dff2ffa232f54595f48c2639d828c4a64ddde`
- 锁定 Frappe HR `2d9e4a0bc7a8d18c42c25cc7ff95eb52b460c6b1`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 版本范围的组织标准映射 DocType | 元数据、蓝图和迁移测试 |
| 服务 | 映射创建修订和日期解析 | 锁定 Bench 测试 |
| 权限 | 三个管理角色写服务；集成角色只读 | 权限先行和直写拒绝测试 |
| API | `CORE-014`、`CORE-015` | 三份 API 目录与 HTTP 测试 |
| 完整性 | 法人、启停、唯一目标和双树关系 | 异常路径集成测试 |
| 并发 | 组织、映射和标准目标行锁；乐观修订 | 冲突和回滚测试 |
| 幂等 | 写入要求请求键和备注摘要 | 重放和同键异参测试 |
| 审计 | 只记录修订、数量和字段名 | 标准名称与备注脱敏测试 |
| 工作区 | 中文标准映射入口 | 元数据与仓库契约 |
| 文档 | ADR、架构、升级和回滚说明 | Git 治理门禁 |

## 实现边界

只修改 `ione_hrp` 的 `HRP Organization` 模块。标准 `Department` 和 `Cost Center`
仅被读取和链接，其创建、修改和树维护仍由 ERPNext 标准控制器负责。不创建第二个 App，
不修改三个锁定上游，不连接生产 Press。

## 验证

```bash
python -m unittest tests.test_organization_mapping -v
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-019 \
  --correlation-id COD-019-local
python scripts/quality.py
```

锁定 Bench 必须运行新增映射集成测试和全量回归，随后重复迁移，并验证 development、
test、demo 三个隔离站点 `Error Log` 为零。

## 迁移与回滚

新增映射表和三个复合唯一约束，不创建或修改 ERPNext 标准记录。升级前备份数据库；回滚
应用提交并 migrate，保留映射表和数据，不执行破坏性删除。

## 实施证据

- GitHub PR：`#25`
- 核心实现提交：`24840fd9478d31f8117e981ee6572eb7335dff40`
- 审计字段修复提交：`763dd1f4f46de871f8175f255c15d6a45397ee67`
- 分支 CI：`30538289925`
- 仓库测试：`214/214`
- Frappe 集成测试：`135/135`，其中组织映射 `10/10`
- 隔离站点：development、test、demo 重复迁移无漂移，三个站点 `Error Log` 均为 `0`
- 安全：Bandit、凭据扫描、pip-audit、npm audit、Grype 和许可证拒绝项均为 `0`
- SBOM：CycloneDX `1.7`，`105` 个组件、`105` 条依赖关系
- 生产边界：未连接或修改生产 Press、Bench、Site 和数据库
