# COD-006 建立开发、测试、演示环境

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/deployment.md`
- `architecture/environments.md`
- `architecture/single_app_architecture.md`
- `resolved_versions.lock.json`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 三类环境 | `development`、`test`、`demo` 配置档 | 唯一 Bench/Site/端口契约 |
| 可重复创建 | `scripts/environment_manager.py provision` | 锁定 bootstrap 与幂等配置 |
| 测试环境 | 测试接口开启、调度/外发关闭 | CI 实际迁移与 App 测试 |
| 演示环境 | 仅合成数据，标准 ERPNext Setup Wizard | 重复初始化不重复建账 |
| 运行时状态 | 认证只读环境 API | 不返回路径、端口或秘密 |
| 权限与异常 | Guest、目标覆盖、生产式目标、配置漂移拒绝 | 单元和集成测试 |
| 审计 | correlation ID 与脱敏 JSONL | 成功/失败事件、`0600` 权限 |

## 模型、服务、权限和迁移

本任务不新增业务 DocType、Fixture 或数据库 Patch。环境身份在数据库连接前
就必须可用，因此由 `site_config.json` 和版本控制中的配置档管理，而不是创建
可被业务用户修改的 DocType。

- 服务：`ione_hrp.services.environment` 校验运行时配置，并提供外部集成闸门。
- API：认证用户只读查询环境状态；Guest 拒绝。
- 写入：仅非生产配置、调度器和演示合成机构初始化。
- 过账：演示初始化调用 ERPNext 标准 Setup Wizard，不直接写 GL Entry、
  Stock Ledger Entry 或 Bin。
- 生产：不连接 Press、不修改 `manager.myyr.top`，无生产迁移。

## 安全与幂等

- 三个环境默认合成数据、无邮件、无外部集成、无公开访问。
- 默认目标均为 `.localhost`；覆盖需显式确认，生产式目标仍失败关闭。
- 同一配置连续应用时第二次 `changed=false`，迁移保持幂等。
- Site 已绑定其他配置档、缺少必需 App、上游 SHA 漂移或调度状态错误时失败。
- 审计不包含密码、令牌、数据库凭据、患者数据或命令输出。
- 环境管理器没有删除命令，避免误删生产或共享数据库。

## 验证

```bash
python scripts/environment_manager.py validate
python scripts/environment_manager.py plan development
python scripts/environment_manager.py plan test
python scripts/environment_manager.py plan demo
python scripts/quality.py
bash -n scripts/ci_integration.sh
```

完成前还必须在 GitHub 隔离 MariaDB Runner 中实际创建三个 Site，验证重复
配置、合成演示初始化、全部 `ione_hrp` 集成测试、三个站点 Error Log 为 0，
并取得 `Required` 成功。

## 回滚

回滚本任务提交不会改变生产数据库。删除非生产 Site 或 Bench 属于独立运维
操作，必须按配置档完整路径人工确认；不得自动递归删除。需要保留的合成数据
先备份，再停机并移除对应环境。
