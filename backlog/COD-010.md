# COD-010 建立审计上下文与关联 ID

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/single_app_architecture.md`
- `architecture/audit_context.md`
- `architecture/adr/ADR-0005-request-and-job-audit-context.md`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 不可变 `AuditContext` 与严格版本化载荷 | 纯单元测试覆盖字段、ID、通道和父子关系 |
| 服务 | HTTP/任务生命周期、统一审计和受控入队公开服务 | 锁定 Frappe 原生钩子与应用服务 |
| 权限 | 诊断 API 仅允许已认证用户 | 管理员成功和 Guest 401 端到端测试 |
| API | `PLT-014` 与两类响应头 | CSV、YAML、OpenAPI 和真实 HTTP 测试 |
| 审计 | 所有自研结构化审计通过标量脱敏入口 | AST 门禁和敏感字段失败关闭测试 |
| 幂等 | 只读查询零数据库写；同一关联下请求 ID 唯一 | Version/Comment/Error Log 计数测试 |
| 异常 | 非法 HTTP 头返回受控 400；非法任务载荷安全替换 | 错误体、响应头和任务载荷测试 |
| 文档 | ADR、架构说明、任务和变更记录 | Git 治理门禁 |

## 模型、服务、权限与迁移

本任务不新增 DocType、Fixture、Patch 或 Workspace。审计上下文是数据库连接
失败前也必须可用的短生命周期元数据，使用源码中的不可变模型可避免生产 Site
出现可编辑的第二份真相。

- `ione_hrp.common.audit_context`：唯一 ID、载荷和日志字段契约；
- `ione_hrp.services.audit_context`：请求/任务钩子、公开服务、脱敏日志和诊断服务；
- `ione_hrp.api.v1.audit`：只读 API；
- `ione_hrp.hooks`：Frappe v17 原生前后钩子；
- 三个工程管理脚本共用纯 Python ID 校验器。

不修改 Frappe、ERPNext 或 HRMS，不写受保护台账，不修改已提交标准单据。

## 安全与审计

调用方只能提供关联 ID，请求 ID 始终由服务端生成。日志和公开响应不含用户、
邮箱、患者、IP、URL、请求正文、文件路径、站点或凭据。任务载荷在目标函数执行
前被消费。非法输入不回显；错误体只返回安全的新上下文和稳定错误码。

## 验证

```bash
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-010 \
  --correlation-id COD-010-local
python scripts/quality.py
bash -n scripts/ci_integration.sh
```

锁定 Bench 集成测试必须覆盖 HTTP 成功、参数优先级、Guest、非法头、错误体、
响应头、只读幂等、队列父子传播、非法载荷和日志脱敏。

## 实施证据

- 实现提交：`41771b79a8f5286292ce6d2c4f253b2bad399e29`
- GitHub Actions：`30463143673`
- Quality、Integration 和 Required 均通过。
- 仓库测试 `116/116`、锁定 Bench Frappe 集成测试 `45/45` 通过。
- HTTP 验证覆盖合法及非法关联 ID、参数优先级、Guest 401、响应头、受控错误体
  和只读幂等。
- 任务验证覆盖父子传播、保留载荷消费和损坏载荷安全替换；服务验证覆盖相邻调用
  隔离及嵌套上下文不可替换。
- CI 首轮发现直接服务调用在同一 Frappe 进程中复用上一条上下文；新增
  `service_audit_scope` 后重跑全部通过。
- 未创建 DocType、Fixture、Patch 或 Workspace，未执行 Press 或生产迁移。

## 回滚

整体回滚 COD-010 的钩子、服务、API 和调用点即可；不存在数据库迁移或业务数据
清理。已产生的外部日志不改写，旧接口参数保持兼容。
