# COD-011 建立领域服务基类

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/single_app_architecture.md`
- `architecture/domain_services.md`
- `architecture/adr/ADR-0006-domain-service-and-durable-idempotency.md`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 不可变服务定义、执行结果和确定性 JSON 契约 | 纯单元测试 |
| 服务 | 权限、校验、savepoint、幂等、审计和异常模板方法 | 领域服务集成测试 |
| 数据 | 内部 `HRP Service Idempotency` DocType | 真实 Site migrate 与控制器测试 |
| 权限 | 系统管理角色只读，应用服务受控写入 | DocType 权限与权限先行测试 |
| API | `PLT-009` 迁移到 HRP Foundation 公共 facade | CSV、YAML、OpenAPI 与 HTTP 测试 |
| 审计 | 启动、完成、重放和失败事件不记录业务载荷 | mock 日志断言与仓库门禁 |
| 幂等 | 键哈希、请求指纹、加密响应快照和冲突检测 | 成功、重放、同键异参和密文断言 |
| 异常 | 受控错误和未知异常均回滚到 savepoint | 异常路径集成测试 |
| 文档 | 架构说明、ADR、任务和变更记录 | Git 治理门禁 |

## 模型、服务、权限与迁移

新增的 DocType 是 `HRP Foundation` 的内部技术数据，不属于 398 个业务设计 DocType。
它不加入 Workspace、Fixture 或全局搜索。标准 Frappe migrate 会同步其表结构，不需要
patch。发布前必须备份数据库和 Site 加密密钥。

模块启停写入从 API 层迁到
`ione_hrp.hrp_foundation.services.module_settings.SetModuleEnabledService`，对外只暴露
`ione_hrp.hrp_foundation.services.set_module_enabled`。API 不再直接保存 DocType。

## 破坏性变更

`PLT-009` 现在要求 `Idempotency-Key`。缺失或非法键返回 `IONE-CORE-0003`，相同键与
不同请求返回 `IONE-CORE-0007`。调用方必须先升级再发布服务端。

## 验证

```bash
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-011 \
  --correlation-id COD-011-local
python scripts/quality.py
bash -n scripts/ci_integration.sh
```

锁定 Bench 集成测试必须覆盖 migrate、真实数据库事务、Site 加密密钥和 HTTP 请求头。

## 实施证据

- 实现提交：`746fd31c3ce647cf3c6323f125259466393fe0d9`
- GitHub Actions：`30470639107`
- Quality、Integration 和 Required 全部通过。
- 仓库测试 `126/126`，锁定 Bench Frappe 集成测试 `54/54` 通过。
- test、development 和 demo Site 均完成 migrate，重复 test 配置返回
  `changed: false`，三类 Site 的 Error Log 均为 `0`。
- 权限、开始/完成/重放/失败审计、同键同参重放、同键异参冲突、缺失和非法键、加密快照、
  篡改关闭、过期复用、查询、未知异常回滚及真实 HTTP 请求头均有自动化验证。
- 未执行 Press 或生产迁移。

## 回滚

先恢复兼容客户端或暂停 `PLT-009` 写入，再回滚应用代码。保留幂等表和有效记录，不在
回滚中删除生产数据；待记录过期并完成备份验证后，再由独立变更决定是否清理表结构。
