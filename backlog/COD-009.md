# COD-009 建立统一异常和错误码

## 设计依据

- `AGENTS.md`
- `Definition_of_Done.md`
- `architecture/single_app_architecture.md`
- `architecture/change_governance.md`
- `architecture/adr/ADR-0003-source-controlled-error-contract.md`
- `architecture/adr/ADR-0004-governance-metadata-risk-neutrality.md`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 统一异常 | `IoneApplicationError` 和唯一 Frappe 抛出适配器 | 所有自研直接 `frappe.throw`/`frappe.only_for` 调用迁移并由 AST 契约禁止回归 |
| 稳定错误码 | `IONE-CORE-0001` 起的源码注册表 | schema、唯一、连续编号、类别和 HTTP 映射测试 |
| API 契约 | `ione_error` 响应体与 `X-Ione-Error-*` 响应头 | 锁定 Bench HTTP 端到端测试 |
| 多语言 | 英文源消息与标准 `translations/zh.csv` | 翻译覆盖和真实中文响应测试 |
| 权限 | 错误目录只限两个管理角色，业务端点保持原边界 | 管理员、Guest 和普通用户测试 |
| 审计与脱敏 | 随机 `error_id` 和结构化安全日志 | 原因消息、业务载荷、路径和用户不进入响应或应用审计 |
| 幂等 | 目录解析和公开摘要确定性，只读查询不写数据库 | 重复摘要、Version/Comment/Error Log 计数测试 |
| 异常路径 | 配置损坏、未知键、非法状态和重试语义失败关闭 | 纯单元测试与 Frappe API 测试 |

## 模型、服务、权限和迁移

本任务不新增 DocType、Fixture、数据库 Patch 或 Workspace。错误码是代码/API
契约，必须在请求处理和数据库迁移之前可用，并在所有站点保持一致，因此使用
Git 中的严格 JSON 注册表，而不是生产可编辑模型。

- 模型：`ione_hrp.common.error_catalog` 解析不可变定义和翻译覆盖。
- 服务：`ione_hrp.services.errors` 是唯一 Frappe 抛出入口与公开服务门面。
- API：管理员只读查询错误目录；不存在新增、修改或删除错误码的 HTTP 接口。
- 权限：`System Manager` 或 `HRP System Manager` 可查目录；原业务接口权限
  不因迁移改变。
- 迁移：无 Schema 和数据迁移，不触发会计、库存或 HR 过账。
- 生产：不连接 Press，不修改 `manager.myyr.top`，不执行生产 migrate。

## 安全、审计与错误契约

公开 `ione_error` 固定包含 `schema_version`、`code`、`category`、`message`、
`error_id` 和 `retryable`。不返回内部 key、原因链、堆栈、文件路径、SQL、
请求参数或用户标识。响应头只包含错误码与随机错误 ID。应用日志仅记录相同
安全元数据和原因类型名称；Frappe 对 500 错误的标准服务端日志策略保持不变。

COD-009 只建立单次错误引用 ID。跨调用关联、请求上下文传播和统一审计上下文
属于依赖本任务的 COD-010，不在本任务提前实现。

## 验证

```bash
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-009 \
  --correlation-id COD-009-local
python scripts/quality.py
bash -n scripts/ci_integration.sh
```

锁定 Bench 集成测试必须覆盖错误目录 HTTP 成功、Guest/普通用户失败、400/401/
403 状态、机器错误体、响应头、中文翻译、脱敏日志和只读幂等。

## 回滚

回滚仅恢复 `ione_hrp` 原异常调用与 API 目录，不修改标准应用源码和 Site
业务数据。已经发布的 `IONE-CORE-xxxx` 不重新分配；后续语义变化新增错误码。
Accepted ADR 和 Git/PR/CI 历史不改写，决策变化由新 ADR 取代。
