# COD-001 建立 mono-repo 与分支保护

## 设计依据

- `AGENTS.md`
- `architecture/single_app_architecture.md`
- `architecture/repository_governance.md`
- `.github/branch-protection.json`

## 交付

- 唯一自研 App 和 Python 包统一为 `ione_hrp`。
- 36 个 Module 继续位于 `ione_hrp` 内，注册表、`modules.txt` 与目录对应。
- 根目录初始化为 Git mono-repo，并以 `main` 作为默认分支。
- 仓库根目录符合 Press App Source 结构，可直接发现 `pyproject.toml` 和 `ione_hrp/hooks.py`。
- GitHub 使用强制 PR、线性历史和讨论解决保护 `main`，禁止管理员直推、强推和删除。
- 当前单维护者阶段保留 `CODEOWNERS`，但不设置无法满足的独立批准门槛。
- `scripts/repository_contract.py` 阻止多 App、旧前缀、上游源码混入和直接写受保护台账。
- 仓库级单元测试验证上述契约；持续集成流水线留待 COD-005。

## 任务适用面

本任务不创建业务 DocType、服务、写 API、Workspace、Fixture 或数据库迁移。
权限、业务审计和业务幂等不适用；仓库治理的异常路径由自动化测试覆盖。

## 验证

```bash
python scripts/repository_contract.py
python scripts/validate_package.py
python scripts/checksums.py
python -m unittest discover -s tests -p "test_*.py"
python -m compileall -q ione_hrp scripts tests
```

## 实际环境结果

- GitHub 仓库：`https://github.com/jerry317395616/ione_hrp`
- 仓库可见性：公开
- `main` 服务端分支保护验证日期：`2026-07-29`
- Press 构建：`golc2jiqdo`
- Deploy Candidate：`deploy-0002-000022`
- Bench：`bench-0002-000022-f1`
- 站点：`manager.myyr.top`
- `ione_hrp 0.1.0` 已通过 Press 原生 Install App 操作安装。
- 健康检查返回 `status=ok`；36 个 Module Def、36 条模块设置和 4 个核心角色已创建。
- 安装后的 Error Log 数量为 0。
- 安装前完整备份位于
  `/home/zyd/backups/ione_hrp-install-20260728_213510`。

## 远端保护结果

- 仓库所有者已明确批准将仓库设置为公开。
- `main` 的 GitHub 服务端分支保护已经应用并通过机器校验。
- 管理员包含在保护范围内，直接推送、强推和删除均被禁止。
- 所有变更必须经 Pull Request，保持线性历史并解决全部评审讨论。
- 仓库当前只有一名协作者，因此必需批准数暂为 0；添加第二名维护者后提升为 1。
- `.githooks/pre-push` 继续作为本地误操作防护，但不替代服务端规则。

## 升级与回滚

- 当前应用已经安装到 Press 管理的 `manager.myyr.top`。
- 回滚前先确认没有新增业务数据，再使用上述安装前备份恢复数据库和附件。
- 禁止直接删除生产 DocType 或手工修改数据库表来回滚安装。
- Git 回滚本任务提交即可恢复源码；GitHub 分支保护需按同一提交中的策略文件同步回滚。
