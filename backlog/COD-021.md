# COD-021 实现外部编码映射

## 设计依据

- `AGENTS.md`
- `architecture/single_app_architecture.md`
- `architecture/external_code_mapping.md`
- `architecture/adr/ADR-0015-service-owned-bidirectional-external-code-mapping.md`
- `design/doctype_catalog.csv`
- `design/field_catalog.csv`
- `doctype_blueprints/hrp_master_data/hrp_external_code_mapping.json`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`
- 锁定 ERPNext `372dff2ffa232f54595f48c2639d828c4a64ddde`
- 锁定 Frappe HR `2d9e4a0bc7a8d18c42c25cc7ff95eb52b460c6b1`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 外部编码双向映射及源/目标身份键 | 运行时元数据、蓝图、设计目录 |
| 服务 | 创建/修订、入站解析、出站解析 | 纯契约和 Frappe 集成测试 |
| 权限 | 治理角色写、集成角色读、Desk 只读 | 权限钩子和越权测试 |
| API | `CORE-004`、`CORE-021`、`CORE-022` | 三份 API 目录和真实 HTTP 测试 |
| 并发 | 行锁、乐观修订、双向数据库唯一约束 | 冲突和重复迁移测试 |
| 幂等 | 写命令要求请求键 | 重放、异参和缺失请求头测试 |
| 审计 | 仅摘要、方向、修订号和变化字段名 | 敏感原文排除测试 |
| 工作区 | 中文“外部编码映射”入口 | Workspace 和 HRP 侧栏测试 |
| 文档 | 架构、ADR、迁移和回滚 | 仓库治理门禁 |

## 实现边界

只修改 `ione_hrp` 的 `HRP Master Data` 模块和公共契约。标准主数据只读校验，不创建、修改或删除
ERPNext 记录。不创建第二个 App，不修改三个锁定上游，不连接生产 Press。

## 验证

```bash
python -m unittest tests.test_external_code_mapping -v
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-021 \
  --correlation-id COD-021-local
python scripts/quality.py
```

锁定 Bench 必须运行 `ione_hrp.hrp_master_data.tests.test_external_code_mapping` 和全量应用测试，并验证
development、test、demo 三个隔离站点重复迁移无漂移且 `Error Log` 为零。

## 迁移与回滚

迁移新增一个 DocType 表、两个摘要身份唯一约束和一个有效期索引，不修改 ERPNext 标准记录。升级前备份
站点数据库；回滚应用提交后迁移，保留表、约束和映射数据，不执行破坏性删除。

## 实施证据

- 分支：`cod-021-external-code-mapping`
- 纯契约测试：待最终验证
- Frappe 集成测试：待 CI 验证
- GitHub PR：待创建
- 生产边界：未连接或修改生产 Press、Bench、Site 和数据库
