# COD-018 实现组织层级与版本

## 设计依据

- `AGENTS.md`
- `architecture/single_app_architecture.md`
- `architecture/organization_hierarchy.md`
- `architecture/adr/ADR-0012-versioned-hospital-organization-hierarchy.md`
- `design/doctype_catalog.csv`
- `design/field_catalog.csv`
- `doctype_blueprints/hrp_organization/`
- 锁定 Frappe `883224ff626c4635a58c47133f1ba6101cdbd938`
- 锁定 ERPNext `372dff2ffa232f54595f48c2639d828c4a64ddde`
- 锁定 Frappe HR `2d9e4a0bc7a8d18c42c25cc7ff95eb52b460c6b1`

## 交付映射

| 验收项 | 实现 | 证据 |
| --- | --- | --- |
| 模型 | 医院、组织版本、组织树节点三个标准 DocType | 元数据与迁移测试 |
| 服务 | 医院修订、版本分配、整树替换、发布、日期查询 | 锁定 Bench 测试 |
| 权限 | 三个管理角色；版本和节点服务写入 | 权限先行与直写拒绝测试 |
| API | `CORE-009` 至 `CORE-013` | 三份 API 目录与 HTTP 测试 |
| 并发 | 医院/版本行锁、单调版本号、乐观修订号 | 冲突与回滚测试 |
| 幂等 | 四个命令均要求请求键和加密结果 | 重放与同键异参测试 |
| 审计 | 只记录版本、修订、节点数、字段名和摘要 | 名称与备注脱敏测试 |
| 工作区 | 中文组织管理入口 | 元数据与仓库契约 |
| 迁移 | 默认医院 Link、旧标识确定性保留、唯一约束 | 重复迁移测试 |
| 文档 | ADR、架构、升级和回滚说明 | Git 治理门禁 |

## 实现边界

只修改 `ione_hrp` 内部 `HRP Organization` 和 COD-017 系统设置兼容层。不创建第二个
App，不修改三个锁定上游。本任务不映射标准 `Department`/`Cost Center`，该能力属于
COD-019。

## 验证

```bash
python -m unittest tests.test_organization -v
python scripts/change_manager.py validate
python scripts/change_manager.py check \
  --base-ref origin/main \
  --task COD-018 \
  --correlation-id COD-018-local
python scripts/quality.py
```

锁定 Bench 必须运行新增组织集成测试和全量回归，随后重复迁移，并验证 development、
test、demo 三个隔离站点 `Error Log` 为零。

## 迁移与回滚

新增三个业务表和两个复合唯一约束。旧默认医院文本转换为确定性医院记录并保留原名称。
升级前备份数据库；回滚时恢复应用提交并 migrate，保留医院、版本和节点表，不直接删除
已发布历史或生产数据。

## 实施证据

- PR：<https://github.com/jerry317395616/ione_hrp/pull/23>
- 实现提交：`16065db77ecd928dc43228fa58476d2a0f783b4b`
- 最终验证提交：`303a4e7c176031cb5f18f1bb34f6fe85f5e8e96c`
- GitHub Actions：<https://github.com/jerry317395616/ione_hrp/actions/runs/30531825570>
- Quality、Security、Integration、Required 四个作业全部通过。
- 仓库门禁测试 `207/207`、锁定 Bench Frappe 集成测试 `125/125`、组织集成测试
  `14/14`。
- test 环境重复配置第二次 `changed=false`；development、test、demo 三个隔离站点
  配置无漂移且 `Error Log` 均为 `0`。
- CycloneDX 1.7 SBOM 含 105 个组件和 105 条依赖；Bandit、Gitleaks、pip-audit、
  npm audit、Grype 和许可证阻断发现均为 `0`。
- 未连接或修改生产 Press；发布仍由用户通过受保护 PR 合并后在 Press 执行。
